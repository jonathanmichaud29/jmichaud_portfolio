
services:
    web:
        build:
            context: .
            target: dev
        env_file: .env
        volumes:
            - ../..:/repo
            - node_modules:/repo/node_modules
        ports:
            - "5173:5173"
        networks:
            - frontend

volumes:
    node_modules: