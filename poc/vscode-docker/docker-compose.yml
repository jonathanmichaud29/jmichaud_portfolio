include:
    - services/api/compose.yml
    - services/worker/compose.yml
    - services/web/compose.yml
    - services/db/compose.yml

networks:
    frontend:
    backend:

volumes:
    node_modules:    # shared across all JS services